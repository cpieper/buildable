<script lang="ts">import type { Substitution } from '$lib/api/types'; let { entries, kind }: { entries: Substitution[]; kind: 'color' | 'equivalent' } = $props();</script>
<div class="stories">{#each entries as item}<div class="story"><div><span class="swatch" style={`background:#${item.required_color.rgb_hex}`} aria-hidden="true"></span><strong>Needs {item.required_color.name}</strong><span>{item.required_part.name} · {item.required_part.part_num}</span></div><div><span class="swatch" style={`background:#${item.supplied_color.rgb_hex}`} aria-hidden="true"></span><strong>Use {item.supplied_color.name}</strong><span>{item.supplied_part.name} · {item.supplied_part.part_num} · ×{item.quantity}</span></div></div>{/each}</div>
<style>.stories { display:grid; gap:8px; }.story { display:grid; grid-template-columns:1fr 1fr; gap:18px; padding:12px; border:1px solid var(--line); background:var(--surface); }.story div { display:grid; grid-template-columns:14px 1fr; gap:2px 7px; align-items:center; font-size:.82rem; }.story span:not(.swatch) { grid-column:2; color:var(--ink-muted); font-size:.75rem; }.swatch { width:14px; height:14px; border:1px solid #777; border-radius:50%; }@media(max-width:560px){.story{grid-template-columns:1fr;gap:12px;}}</style>
