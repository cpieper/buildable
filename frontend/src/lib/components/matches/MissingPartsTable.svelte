<script lang="ts">import type { MissingRequirement } from '$lib/api/types'; let { items }: { items: MissingRequirement[] } = $props();</script>
<div class="missing-list">{#each items as item}<div class="missing"><div class="part-image" aria-hidden="true">▦</div><div><strong>{item.part_name}</strong><span>{item.part_num}</span></div><div><span class="swatch" aria-hidden="true"></span>{item.color_name}</div><strong>×{item.quantity}</strong></div>{/each}</div>
<style>.missing-list { border:1px solid var(--line); background:var(--surface); }.missing { display:grid; grid-template-columns:38px 1fr minmax(130px,.5fr) 45px; align-items:center; gap:10px; padding:9px 12px; border-bottom:1px solid var(--line); font-size:.8rem; }.missing:last-child { border-bottom:0; }.part-image { display:grid; place-items:center; width:34px;height:34px;background:#ece6da;border-radius:4px; }.missing div:nth-child(2) { display:grid; gap:2px; }.missing div:nth-child(2) span { color:var(--ink-muted); font-size:.72rem; }.swatch { display:inline-block;width:12px;height:12px;margin-right:6px;border:1px solid #777;border-radius:50%;background:#ddd; }@media(max-width:560px){.missing{grid-template-columns:34px 1fr 40px}.missing div:nth-child(3){grid-column:2;}.missing>strong{grid-column:3;grid-row:1 / span 2;}}</style>
