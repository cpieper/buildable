<script lang="ts">import { Boxes, Package, Hammer, Settings } from 'lucide-svelte'; let { pathname }: { pathname: string } = $props(); const navigation = [{ href: '/collection', label: 'Collection', icon: Boxes }, { href: '/inventory', label: 'Inventory', icon: Package }, { href: '/buildable', label: 'Buildable Sets', icon: Hammer }, { href: '/settings', label: 'Settings', icon: Settings }];</script>
<aside class="workshop-rail"><a class="wordmark" href="/buildable" aria-label="What2Build home">What2Build</a><p class="rail-kicker">Workshop</p><nav aria-label="Primary">{#each navigation as item}<a href={item.href} aria-current={pathname === item.href ? 'page' : undefined}><item.icon size={18} strokeWidth={2.25} /><span>{item.label}</span></a>{/each}</nav></aside>
<style>.workshop-rail { box-sizing:border-box; position:fixed; inset:0 auto 0 0; width:224px; padding:24px 16px; border-right:1px solid var(--line); background:var(--surface); }.wordmark { color:var(--ink); font-weight:800; font-size:1.25rem; letter-spacing:-.04em; text-decoration:none; }.rail-kicker { margin:24px 8px 8px; color:var(--ink-muted); font-size:.6875rem; font-weight:700; letter-spacing:.12em; text-transform:uppercase; }nav { display:grid; gap:4px; }nav a { display:flex; align-items:center; gap:10px; min-height:40px; padding:0 10px; border-radius:6px; color:var(--ink); font-size:.875rem; font-weight:650; text-decoration:none; }nav a:hover { background:var(--paper); }nav a[aria-current='page'] { background:var(--red); color:white; }</style>
