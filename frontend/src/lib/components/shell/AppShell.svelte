<script lang="ts">import type { Snippet } from 'svelte'; import WorkshopRail from './WorkshopRail.svelte'; import MobileNav from './MobileNav.svelte'; import StatusStud from './StatusStud.svelte'; let { pathname, children }: { pathname: string; children: Snippet } = $props();</script>
<WorkshopRail {pathname} /><div class="shell-content"><header class="shell-status"><StatusStud tone="success" label="Workshop unlocked" /><span>Workshop unlocked</span></header><main>{@render children()}</main></div><MobileNav {pathname} />
<style>.shell-content { min-height:100dvh; margin-left:224px; }.shell-status { display:flex; align-items:center; justify-content:flex-end; gap:7px; min-height:44px; padding:0 28px; border-bottom:1px solid var(--line); color:var(--ink-muted); font-size:.75rem; font-weight:650; }main { max-width:1100px; margin:0 auto; padding:28px; }@media (max-width:719px) { .shell-content { margin-left:0; padding-bottom:56px; }.shell-status { padding:0 16px; }main { padding:20px 16px; }:global(.workshop-rail) { display:none; } }</style>
