<script lang="ts">import { Boxes, Package, Hammer, Settings } from 'lucide-svelte'; import { isRouteActive } from '$lib/navigation/routes'; let { pathname }: { pathname: string } = $props(); const navigation = [{ href: '/collection', label: 'Collection', icon: Boxes }, { href: '/inventory', label: 'Inventory', icon: Package }, { href: '/buildable', label: 'Buildable Sets', icon: Hammer }, { href: '/settings', label: 'Settings', icon: Settings }];</script>
<nav class="mobile-nav" aria-label="Mobile navigation">{#each navigation as item}<a href={item.href} aria-current={isRouteActive(item.href, pathname) ? 'page' : undefined} aria-label={item.label} title={item.label}><item.icon size={20} strokeWidth={2.25} /></a>{/each}</nav>
<style>.mobile-nav { display:none; }@media (max-width:719px) { .mobile-nav { position:fixed; z-index:2; right:0; bottom:0; left:0; display:flex; height:56px; justify-content:space-around; border-top:1px solid var(--line); background:var(--surface); }.mobile-nav a { display:grid; width:56px; place-items:center; color:var(--ink-muted); }.mobile-nav a[aria-current='page'] { color:var(--red); } }</style>
